# 测试样例目录

这里用于保存可复跑的输入样例、基础数据样例和期望结果样例。

建议按业务场景建子目录，例如：

```text
tests/fixtures/
  lingxing/
    one_sku/
    ups_packing/
    mixed_batch/
```

注意：

- 不要提交账号、密码、Cookie、客户隐私或真实敏感数据。
- 如果必须保留真实样例，先做脱敏。
- 大体积样例文件应先确认是否适合进入 Git。

